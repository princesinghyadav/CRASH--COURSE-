import { Auth } from '../src/App';
 
import React,  { useContext } from 'react';

// const valu= React.useContext(Auth);
// const { isLoggedIn } = useContext(Auth );
export   function Topmainright(){
// console.log(isLoggedIn)
    return (
        <div> 
<h3> topmainright </h3>

        </div>
    )
}